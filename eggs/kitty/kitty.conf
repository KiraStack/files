# Appearance
font_family JetBrainsMono Nerd Font
font_size 12
bold_font auto
italic_font auto
bold_italic_font auto
background_opacity 0.7
dynamic_background_opacity yes
window_padding_width 10
hide_window_decorations yes
initial_window_width 950
initial_window_height 500

# Cursor Behavior
cursor_shape beam
cursor_blink_interval 0.5
cursor_stop_blinking_after 1
cursor_trail 1

# Input
scrollback_lines 2000
wheel_scroll_min_lines 1
selection_foreground none
selection_background none
enable_audio_bell no
confirm_os_window_close 0

# Prompt
shell fish --login --interactive --init-command "echo \"Hello, $USER! Welcome to your system.\" && echo \"Current directory: $(pwd)\""

# `include` directive can be used to include additional configuration files.
# Relative paths are searched either in the parent of this configuration file
# (when the configuration is loaded through a symlink, the symlink will be
# resolved first) or in $XDG_CONFIG_HOME/kitty/include.
#
# include "extra.conf"
